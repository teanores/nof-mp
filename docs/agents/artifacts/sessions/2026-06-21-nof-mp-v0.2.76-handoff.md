# NOF MP Session Handoff - 2026-06-21

## Current State

- Service: `nof-mp`
- Active delivery context: continue `NOF-MP-SPRINT-2`
- Latest deployed release: `v0.2.76`
- Release commit: `38faef8` (`release: prepare v0.2.76`)
- Hotfix commit: `7251beb` (`fix: keep product launch redirects relative`)
- Deploy path used: owner-approved `manual release-builder`
- Production Helm revision: `107`
- Production image: `localhost:32000/nof-mp:38faef8`
- Rollout: successful

## Completed In This Session

- Fixed `NOF-MP-22`: product launch redirect leaked internal origin `0.0.0.0:3000`.
- Root cause: `apps/web/app/products/[productKey]/launch/route.ts` built absolute redirects from `request.nextUrl.origin`, which can be the internal ingress origin in production.
- Fix: OAuth-managed product launch redirects now return relative `Location` values:
  - `/services/task-tracker?launch=oauth`
  - `/services/habit-tracker?launch=oauth`
- Tracker evidence recorded through `nof-mp` MCP; `NOF-MP-22` marked `done`.

## Verification

- `npm --workspace @nof/nof-mp-web run test -- product-launch-route.test.ts --run`: passed, 5/5.
- `just check`: passed.
  - lint passed
  - typecheck passed
  - tests passed: 73 files / 302 tests
- Public smoke after deploy:
  - `https://forgath.ru/services/task-tracker?launch=oauth`: 200
  - `https://forgath.ru/services/habit-tracker?launch=oauth`: 200
  - unauthenticated launch routes redirect relatively to `/login...`
  - no `0.0.0.0` leak observed in smoke

## Owner UAT Still Needed

Run while logged in to `https://forgath.ru`:

1. Open `https://forgath.ru/products/nof-tt/launch?next=%2Fprojects`
   - Expected: no `0.0.0.0`, no raw JSON, normal Task Tracker launch/OAuth flow.
2. Open `https://forgath.ru/products/nof-ht/launch?next=%2F`
   - Expected: no `0.0.0.0`, normal Habit Tracker launch/OAuth flow.
3. Open `https://forgath.ru/services/task-tracker` and press the product launch button.
   - Expected: normal product flow, no internal host and no raw JSON.

## Important Holds / Constraints

- `NOF-MP-12` remains on HOLD until `nof-tt` closes the separate MCP token secret work (`NOF-TT-185`).
- Continue sprint work only after revalidating the next task against current code, tracker state and live state when production behavior is involved.
- Do not change footer, profile navigation, explanatory UI blocks or auth/session behavior unless the active task explicitly requires it and owner copy/UX is approved.

## Next Recommended Step

Start tomorrow by collecting owner UAT feedback for `v0.2.76`.

If UAT passes, continue `NOF-MP-SPRINT-2` with the next ready task from the sprint packet. If UAT fails, file or update the defect first and keep the sprint scope explicit.
